name: Test
on:
  workflow_call:
    inputs:
      disable_docker_cache:
        required: false
        description: Whether to disable the docker cache or not.
        type: boolean
        default: false
      disable_qemu:
        required: false
        description: Whether to disable the static qemu binaries or not (required for docker multiplatform building).
        type: boolean
        default: false
    secrets:
      username:
        required: true
        description: The github username.
      password:
        required: true
        description: The github password/token.
jobs:
  test:
    name: Testing
    runs-on: ubuntu-20.04
    steps:
      - name: Set up QEMU
        if: ${{ !inputs.disable_qemu }}
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Enable docker cache
        if: ${{ !inputs.disable_docker_cache }}
        uses: satackey/action-docker-layer-caching@v0.0.11
        continue-on-error: true
        with:
          key: ${{ github.repository }}-docker-cache-{hash}
          restore-keys: |
            ${{ github.repository }}-docker-cache-
      - name: Login into docker
        uses: azure/docker-login@v1
        with:
          login-server: ghcr.io
          username: ${{ secrets.username }}
          password: ${{ secrets.password }}
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Make all
        env:
          GITHUB_TOKEN: ${{ secrets.password }}
          DOCKER_USERNAME: ${{ secrets.username }}
          DOCKER_PASSWORD: ${{ secrets.password }}
          IMAGE_BUILDER_CMD_EXTRA_ARGS: --no-cache
        run: make
